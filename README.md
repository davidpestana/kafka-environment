# KAFKA ENVIRONMENT FOR PRACTICES

> make use

Install "make"

```
    sudo apt update
    sudo apt install make
```

Use Commands.
```
    make [command]
```



Envia  mensaje a topic A,B ( con estas reglas cabeceras condiciones KEYS )
